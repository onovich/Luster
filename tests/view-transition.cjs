const {chromium,launchOptions}=require('./support/browser.cjs');const assert=require('node:assert/strict');
(async()=>{const b=await chromium.launch(launchOptions);try{const p=await b.newPage();await p.goto(process.env.DEMO_URL||'http://127.0.0.1:8798',{waitUntil:'domcontentloaded',timeout:120000});await p.waitForFunction(()=>window.foilDemo?.state().ready,null,{timeout:120000});const before=await p.evaluate(()=>foilDemo.state().parameters);await p.locator('#view-material').click();await p.waitForFunction(()=>{const o=+getComputedStyle(document.querySelector('#stage')).opacity;return o>0&&o<1});await p.waitForFunction(()=>document.querySelector('#book').classList.contains('material'));await p.waitForFunction(()=>!document.body.dataset.viewTransition);assert.equal(await p.locator('#stage').evaluate(e=>getComputedStyle(e).opacity),'1');await p.locator('.material-thumb').nth(5).click();await p.locator('#view-book').click();await p.waitForFunction(()=>!document.body.dataset.viewTransition);assert(!(await p.locator('#materialGallery').isVisible()));await p.evaluate(()=>{document.querySelector('#view-material').click();document.querySelector('#view-book').click();});await p.waitForFunction(()=>!document.body.dataset.viewTransition);assert.equal(await p.locator('#view').inputValue(),'book');await p.locator('#view-material').click();await p.waitForFunction(()=>!document.body.dataset.viewTransition);assert(await p.locator('#slot5').isVisible());assert.deepEqual(await p.evaluate(()=>foilDemo.state().parameters),before);await p.emulateMedia({reducedMotion:'reduce'});await p.locator('#view-book').click();await p.waitForFunction(()=>!document.body.dataset.viewTransition);assert.equal(await p.locator('#stage').evaluate(e=>e.getAnimations().length),0);console.log('PASS: both view fades, rapid reversal, selected material and parameters retained, reduced-motion cleanup');}finally{await b.close()}})().catch(e=>{console.error(e);process.exitCode=1});
